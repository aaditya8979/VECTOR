[pytest]
testpaths = tests
markers =
    requires_model: requires Qwen model weights (auto-skip in CI)
    requires_ollama: requires running Ollama server on port 11434
    slow: takes >10s to run — skip with: pytest -m "not slow"
addopts = -v --tb=short
